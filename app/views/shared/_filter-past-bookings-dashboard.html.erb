<!--Dashboard Filter of upcoming bookings-->
<div class="mt-3">
  <ul class="nav nav-pills center-pills mb-3" id="pills-tab" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="pills-past-all-tab" data-toggle="pill" href="#pills-past-all" role="tab" aria-controls="pills-all2" aria-selected="true">All</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="pills-past-week-tab" data-toggle="pill" href="#pills-past-week" role="tab" aria-controls="pills-home" aria-selected="false">Week</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="pills-past-month-tab" data-toggle="pill" href="#pills-past-month" role="tab" aria-controls="pills-profile" aria-selected="false">Month</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="pills-past-year-tab" data-toggle="pill" href="#pills-past-year" role="tab" aria-controls="pills-contact" aria-selected="false">Year</a>
    </li>
  </ul>

  <div class="tab-content" id="pills-tabContent">

    <!--    All Property past bookings -->
    <div class="tab-pane fade show active" id="pills-past-all" role="tabpanel" aria-labelledby="pills-past-all-tab">

      <h5 class="font-weight-bold"> # <%= pluralize(current_user.past_bookings.size, "Booking") %> </h5>

      <% @past_bookings.each do |booking| %>
        <div class="card card-dashboard">
          <div class="card-body">
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-calendar fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%d/%m/%Y") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-clock fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%H:%M") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="fas fa-map-marker-alt fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.property.address %></h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"> <i class="fas fa-euro-sign fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.cost.round %> </h3>
            </div>

            <h4 class="mt-3 mb-1">Service details:</h4>
            <% booking.booking_tasks.each do |t| %>
              <h4 class="mb-1"> <i class="fas fa-check grey"></i> <%= t.task.title %></h4>
            <% end %>

            <% if booking.review %>
              <h4 class="mt-3 mb-1"> Your rating:</h4>
              <% booking.review.rating.times do |i| %>
                <i class="fas fa-star"></i>
              <% end %>
              <% (5 - booking.review.rating).times do |i| %>
                <i class="far fa-star"></i>
              <% end %>
            <% end %>
            <%= link_to "Clean again", new_property_booking_path(booking.property), data: {turbolinks: false}, class:"btn-dark mt-4"%>

            <% if booking.review == nil %>
              <%= link_to "Leave a review", new_booking_review_path(booking), class:"btn-green mt-2"%>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Property past bookings by current WEEK -->
    <div class="tab-pane fade" id="pills-past-week" role="tabpanel" aria-labelledby="pills-past-week-tab">

      <h5 class="font-weight-bold"> # <%= pluralize(current_user.past_bookings_week_pm.size, "Booking") %> </h5>

      <% @past_bookings_week_pm.each do |booking| %>
        <div class="card card-dashboard">
          <div class="card-body">
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-calendar fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%d/%m/%Y") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-clock fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%H:%M") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="fas fa-map-marker-alt fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.property.address %></h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"> <i class="fas fa-euro-sign fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.cost.round %> </h3>
            </div>


            <h4 class="mt-3 mb-1">Service details:</h4>
            <% booking.booking_tasks.each do |t| %>
              <h4 class="mb-1"> <i class="fas fa-check grey"></i> <%= t.task.title %></h4>
            <% end %>

            <% if booking.review %>
              <h4 class="mt-3 mb-1"> Your rating:</h4>
              <% booking.review.rating.times do |i| %>
                <i class="fas fa-star"></i>
              <% end %>
              <% (5 - booking.review.rating).times do |i| %>
                <i class="far fa-star"></i>
              <% end %>
            <% end %>
            <%= link_to "Clean again", new_property_booking_path(booking.property), data: {turbolinks: false}, class:"btn-dark mt-4"%>

            <% if booking.review == nil %>
              <%= link_to "Leave a review", new_booking_review_path(booking), class:"btn-green mt-2"%>
            <% end %>
          </div>
        </div>
      <% end %>

    </div>

    <!-- Property past bookings by current MONTH -->
    <div class="tab-pane fade" id="pills-past-month" role="tabpanel" aria-labelledby="pills-past-month-tab">

      <h5 class="font-weight-bold"> # <%= pluralize(current_user.past_bookings_month_pm.size, "Booking") %> </h5>

      <% @past_bookings_month_pm.each do |booking| %>
        <div class="card card-dashboard">
          <div class="card-body">
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-calendar fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%d/%m/%Y") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-clock fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%H:%M") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="fas fa-map-marker-alt fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.property.address %></h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"> <i class="fas fa-euro-sign fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.cost.round %> </h3>
            </div>


            <h4 class="mt-3 mb-1">Service details:</h4>
            <% booking.booking_tasks.each do |t| %>
              <h4 class="mb-1"> <i class="fas fa-check grey"></i> <%= t.task.title %></h4>
            <% end %>

            <% if booking.review %>
              <h4 class="mt-3 mb-1"> Your rating:</h4>
              <% booking.review.rating.times do |i| %>
                <i class="fas fa-star"></i>
              <% end %>
              <% (5 - booking.review.rating).times do |i| %>
                <i class="far fa-star"></i>
              <% end %>
            <% end %>
            <%= link_to "Clean again", new_property_booking_path(booking.property), data: {turbolinks: false}, class:"btn-dark mt-4"%>

            <% if booking.review == nil %>
              <%= link_to "Leave a review", new_booking_review_path(booking), class:"btn-green mt-2"%>
            <% end %>
          </div>
        </div>
      <% end %>

    </div>

    <!--Property past bookings by current YEAR -->
    <div class="tab-pane fade" id="pills-past-year" role="tabpanel" aria-labelledby="pills-past-year-tab">

      <h5 class="font-weight-bold"># <%= pluralize(current_user.past_bookings_year_pm.size, "Booking") %> </h5>

      <% @past_bookings_year_pm.each do |booking| %>
        <div class="card card-dashboard">
          <div class="card-body">
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-calendar fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%d/%m/%Y") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="far fa-clock fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.date.strftime("%H:%M") %> </h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"><i class="fas fa-map-marker-alt fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.property.address %></h3>
            </div>
            <div class="card-item">
              <div class="card-icon-container"> <i class="fas fa-euro-sign fa-fw"></i></div>
              <h3 class="ml-3"><%= booking.cost.round %> </h3>
            </div>


            <h4 class="mt-3 mb-1">Service details:</h4>
            <% booking.booking_tasks.each do |t| %>
              <h4 class="mb-1"> <i class="fas fa-check grey"></i> <%= t.task.title %></h4>
            <% end %>

            <% if booking.review %>
              <h4 class="mt-3 mb-1"> Your rating:</h4>
              <% booking.review.rating.times do |i| %>
                <i class="fas fa-star"></i>
              <% end %>
              <% (5 - booking.review.rating).times do |i| %>
                <i class="far fa-star"></i>
              <% end %>
            <% end %>
            <%= link_to "Clean again", new_property_booking_path(booking.property), data: {turbolinks: false}, class:"btn-dark mt-4"%>

            <% if booking.review == nil %>
              <%= link_to "Leave a review", new_booking_review_path(booking), class:"btn-green mt-2"%>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</div>
